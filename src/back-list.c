/* back-list - lists current status of the build tree */

#include <stdio.h>
#include "back.h"

int back_list(UNUSED int argc, UNUSED char** argv) {
  BACK_ERR("subcommand 'list' not yet implemented");
  return -1;
}
