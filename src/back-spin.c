/* back-spin - monitors the filesystem and updates on changes */

#include <stdio.h>
#include "back.h"

int back_spin(UNUSED int argc, UNUSED char** argv) {
  BACK_ERR("subcommand 'spin' not yet implemented");
  return -1;
}
